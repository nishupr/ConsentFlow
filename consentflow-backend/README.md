# ConsentFlow Backend

This is the backend service for ConsentFlow, a consent-enforcement middleware for AI pipelines. 

## Run the server

```bash
uv run uvicorn consentflow.app.main:app --reload
```
