apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # OTel Collector exposes Prometheus metrics on port 8889
    url: http://otel-collector:8889
    isDefault: true
    editable: true
    jsonData:
      timeInterval: "15s"
